<UserControl x:Class="KidsOrganizationApp.UI.View.UserControls.MyTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KidsOrganizationApp.UI.View.UserControls"
             mc:Ignorable="d" 
             d:Height="100" d:DesignWidth="250">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="tBox" VerticalAlignment="Center" FontSize="16" Text="{Binding PlaceHolder}"
                 Foreground ="Black" TextChanged="tBox_TextChanged" Background="Transparent"/>
        <TextBlock x:Name="tbText" VerticalAlignment="Center"
                 Foreground ="Gray" Text="Text" Panel.ZIndex="-1"/>
        <TextBlock x:Name="tbShowText" VerticalAlignment="Center" Grid.Row="1"
                   Text="{Binding PlaceHolder}"/>
    </Grid>
</UserControl>
